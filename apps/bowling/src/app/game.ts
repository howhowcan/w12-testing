export class Game {
    // private _score = 0;
    roll(pins: number) {
        // this._score += pins;
        throw new Error('Method not implemented');
    }

    get score() {
      return 0;
    }
}
